<div class="comment">
  <a href="#" class="avatar">
    <img alt="User's avatar." src="
    <% if(Object.keys(comments).length>0){ %>
      <%= comment.author.avatar %>
    <% } else {} %>
    ">
  </a>
  <div class="content">
    <a href="#" class="author">
      <% if(Object.keys(comments).length>0){ %>
        <%= comment.author.username %>
      <% } else {} %>
    </a>
      <div status='data-notFormated' class="metadata date" style="display: none;">
        <% if(Object.keys(comments).length>0){ %>
          <%= comment.date %>
        <% } else {} %>
      </div>
    <div class="dropdown show button-CommentMenu">
      <button class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="angle down icon"></i>
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <a class="dropdown-item" href="#">Edytuj</a><br>
        <button type="button" id="button-DeleteComment" data="
          <% if(Object.keys(comments).length>0){ %>
            <%=comment._id %>
          <% } else {} %>
        " class="dropdown-item" href="#">Usuń</button>
      </div>
    </div>
    <div class='starsRated'>
      <% if(Object.keys(comments).length>0){ %>
        <% for(var i=0; i<comment.rating; i++){ %>
          <span>&#9733;</span>
        <% } %>
      <% } else {} %>
    </div>
    <div class="text">
      <% if(Object.keys(comments).length>0){ %>
        <%= comment.text %>
      <% } else {} %>    
    </div>
  </div>
</div>